<%= form_for(@schedule) do |f| %>
	<% if @schedule.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

			<ul>
				<% @schedule.errors.full_messages.each do |message| %>
					<li><%= message %></li>
				<% end %>
			</ul>
		</div>
  	<% end %>

	<div class="field">
		<%= f.label "Game Time" %><br>
		<%= f.text_field :gametime %>
	</div>
	<div class="field">
		 <%= f.label "Home Team" %><br>
		 <%= f.text_field :hteam %>
	</div>
	<div class="field">
		 <%= f.label "Away Team" %><br>
		 <%= f.text_field :ateam %>
	</div>
	<div class="field">
		 <%= f.label "Division" %><br>
		 <%= f.select(:division_type, options_for_select([['U8', 'U8'], ['U11', 'U11'], ['U13', 'U13']])) %>
	</div><br>
	<div class="actions">
	 	<%= f.submit %>
	</div>
<% end %>
